#include <bits/stdc++.h>
using namespace std;
using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;

using u128 = unsigned __int128;
using i128 = __int128;

void solve() {
    int r, g, b; cin >> r >> g >> b;
    int lo = 1, hi = (r + g + b);
    while(lo <= hi) {
        int m = lo + (hi - lo) / 2;
        1022
    }
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int tt = 1; cin >> tt;
    while(tt--) solve();
}
