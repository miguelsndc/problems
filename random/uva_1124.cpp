#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;

int main() {
    char str[10000];
    while (gets(str)) {
        printf("%s\n", str);
    }
    return 0;
}